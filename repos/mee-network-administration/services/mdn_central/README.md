# MDN Central cloud service

## Running (dev)

- init db
  - `make run_db_daemon` (if not started)
  - `make create_db` (if not created)
- init crypto material
  - `make gen_dev_mee_signature` (if not generated)
- run main binary
  - `make dev`
